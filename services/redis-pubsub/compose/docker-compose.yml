# ═══════════════════════════════════════════════════════════════
# Redis Pub/Sub — BASE Compose (common service definition)
# NOTE: No persistent volume — pub/sub is ephemeral
# ═══════════════════════════════════════════════════════════════

services:
  redis-pubsub:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - ../env/${APP_ENV:-prod}.env
    volumes:
      - ${DATA_PATH_BASE}/rp_data:/data
      - ../config/base/redis.conf:/etc/redis/base.conf:ro
      - ../config/${APP_ENV:-prod}/redis.conf:/etc/redis/override.conf:ro
      - ../scripts/healthcheck.sh:/usr/local/bin/healthcheck.sh:ro
    entrypoint: ["/usr/local/bin/custom-entrypoint.sh"]
    healthcheck:
      test: ["CMD-SHELL", "/usr/local/bin/healthcheck.sh"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: ${HEALTHCHECK_TIMEOUT:-10s}
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: ${HEALTHCHECK_START:-10s}
    labels:
      com.project.service: "redis-pubsub"
      com.project.managed-by: "docker-compose"
    networks:
      - rp_internal
      - gateway

networks:
  rp_internal:
    external: true
  gateway:
    external: true
